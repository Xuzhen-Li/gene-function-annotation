# 交接指针 — 结构 → 功能

英文卡片：[`../HANDOFF_STRUCTURE_TO_FUNCTION.md`](../HANDOFF_STRUCTURE_TO_FUNCTION.md)。

从结构放行拷进本仓 `local.env`：

1. **`RELEASE_TAG`** = 结构 `release/<TAG>/` 同名  
2. **`PROTEINS_FA`** = `…/release/<TAG>/proteins.faa`（稳定别名；勿用过期根副本）  
3. **`structure_grade`** = 照抄结构仓的 `grade=` 与 `status=`；勿用临时（`grade=L0`）GFF 自称 `grade=F-L1`

上游 [A6](https://github.com/Xuzhen-Li/gene-structure-annotation/blob/main/pipeline/A6_handoff_to_function.md) · 本仓 [FAQ 编号](FAQ_入门.md) · [POST_STRUCTURE](../POST_STRUCTURE.md)。
