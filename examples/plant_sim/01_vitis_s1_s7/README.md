# Plant sim — Vitis F1 + F8 (teaching)

Short classroom pack. Copy `answers.sim.yaml` (and peek `env.snippet` / `plan.md`).

- **F1 ≠ F8.** F1 = default paper frame (DIAMOND + eggNOG + InterProScan). F8 = optional NLR **candidate screen** (add-on), not a second frame.
- **Handoff from structure S7:** run after structure `01_vitis_s1_s7` (S1+S7). S7a window prep can precede F8; do not confuse structure S7 with FA F8.
- **Classroom = plan only** — tonight stop is `my_fa_plan.md` (± DB path list). Do **not** download genomes/DBs or run IPS in class.
- `want_nlr: true` → plan shows F8 after merge; skipping F8 still allows F-L1 if F1 spine + METHODS are honest.

See also: Wiki [Red-lines F8](https://github.com/Xuzhen-Li/gene-function-annotation/wiki/Red-lines#f8-screen), structure sibling `examples/plant_sim/01_vitis_s1_s7/`.
