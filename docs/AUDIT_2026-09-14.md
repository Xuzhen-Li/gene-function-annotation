# Audit 2026-09-14 — gene-function-annotation

Scope: identity/layer split, front door, consistency, teaching spot-check, hygiene.  
Method: greps + Python relative-link check under `README.md` + `docs/**/*.md`.  
Policy: fix **clear bugs** only. No curriculum rewrite.

## Pass / Fail

| Area | Result | Notes |
|------|--------|-------|
| A. Identity / layer split | **Pass** | FA owns F* runners + `functional_master.tsv`. `docs/steps/MAIN.md` redirects structure spine to sibling. No A*/Asm pipeline mirrors under `pipeline/`. |
| B. Front door / usability | **Pass** (after fixes) | Bilingual banner + 3-step path. zh curriculum 00–15 + 99 matches disk. Relative links: **0 broken** after fixes (was 1). |
| C. Consistency | **Pass** | `flow.py` `choose_frame` matches ROADMAP (F5/F3/F2/F1 + add-ons F4/F6/F7/F8/F9). zh/10 F-L0/F-L1 matches English EVALUATION. BILINGUAL.md coherent. |
| D. Teaching quality | **Pass** | WHY pages exist (00, 13–15). Lessons 00–13 have `## 为什么` (14–15 use H1 WHY). No empty stubs. |
| E. Hygiene | **Pass-with-notes** | `config/example.env` is FA-only (explicitly no EVM/BRAKER/S14). Figures: PNG 1.1M + SVG 2.5M under `docs/figures/` (intentional overview art, not accidental data dumps). |

**Overall grade: pass-with-notes**

## Findings (pre-fix)

### Broken relative links (1)

| File | Bad target | Cause |
|------|------------|-------|
| `docs/RECENT_HIGH_QUALITY.md` | `tools/lifton.md` | LiftOn is a **structure** tool; page does not exist in FA |

### Identity

- Pipeline is F1–F9 + merge/release only — no Asm*/A0–A5 structure scripts.
- `example.env` documents sibling for structure; no EVM weights files in `config/`.

### Consistency notes

- Historical naming: `F2_eggnog.sh` is both F1 step and F2 frame core — documented in ROADMAP; not a defect.
- `prefer_fast` forces F-L0 — matches EVALUATION slim grade.

## Fixes applied

1. Retargeted LiftOn link in `RECENT_HIGH_QUALITY.md` to structure repo `docs/tools/lifton.md` (GitHub).
2. Added this audit; linked from README docs table.

## Deferred

- Compressing / regenerating the 2.5M SVG overview (keep source drawio; optional later).
- No alternate-frame curriculum rewrite; F2/F3/F5 docs left as-is.
